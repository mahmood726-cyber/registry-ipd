{
  "name": "registry-ipd",
  "version": "0.1.0",
  "description": "Registry-native pseudo-IPD reconstructor (ClinicalTrials.gov / AACT only, fully offline browser).",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/engine.spec.js validate/metrics.spec.js",
    "fixtures": "node test/gen_fixtures.js",
    "examples": "node examples/gen_examples.js",
    "build": "node build.js",
    "smoke": "python test/smoke_browser.py",
    "evidence": "node validate/evidence_summary.js",
    "verify:headlines": "node validate/verify_headlines.js",
    "verify": "npm test && node validate/verify_headlines.js && npm run evidence"
  },
  "private": true
}
