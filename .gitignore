# local state / progress (may contain local paths)
PROGRESS.md
sentinel-findings.*
STUCK_FAILURES.*

# AACT snapshots are large — never commit
*.zip
*_pipe-delimited-export*/
coverage_report.json
NCT*.json

# python / node
__pycache__/
*.pyc
.pytest_cache/
node_modules/

# os
Thumbs.db
.DS_Store

# bulk harvested cohort (keep only curated demos)
cohort/

# downloaded public validation datasets (not redistributed)
realipd/*.csv

# credentialed IPD exports (DUA-protected) — NEVER commit
realipd/credentialed/

# cached PubMed abstracts (public, but bulky)
.pubmed_cache.json

# enriched cohort (derived from gitignored cohort/ + cached abstracts)
cohort_enriched/
